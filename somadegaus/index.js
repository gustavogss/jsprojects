/**
 * Soma de Gaus 
 * Soma dos numeros até  
 * o valor máximo
 *  */
function somaDeGauss(numeroMaximo) {
  var soma =0;
  for (var i=1; i<=numeroMaximo;i++){
    soma +=i; //soma = soma+i
  }
  return soma;
}
